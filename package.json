{
  "name": "peek-length-prefixed-message",
  "description": "Get the first length-prefixed-message while returning an unconsumed stream",
  "repository": "juliangruber/peek-length-prefixed-message",
  "license": "MIT",
  "scripts": {
    "test": "tape test.js && standard"
  },
  "dependencies": {
    "length-prefixed-message": "^3.0.3",
    "once": "^1.3.3"
  },
  "devDependencies": {
    "collect-stream": "^1.1.1",
    "standard": "^9.0.2",
    "tape": "^4.6.0",
    "varint": "^5.0.0"
  },
  "version": "1.0.2"
}
